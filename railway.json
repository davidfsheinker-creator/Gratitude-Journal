{
  "$schema": "https://schema.railway.app/v1.json",
  "deploy": {
    "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter !@workspace/mockup-sandbox -r --if-present run build",
    "startCommand": "pnpm --filter @workspace/db run push && pnpm --filter @workspace/api-server run start"
  }
}
